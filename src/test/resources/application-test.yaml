spring:
  flyway:
    schemas: DataBaseSocialBoard
    create-schemas: true
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    user: sa
    password: sa
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password: sa

logging.level.sql=debug
